name: Hey CICD
# on:
#   push:
#     branches:
#       - main
#       - feature/*
on:
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest # Runners are the virtual machines where your workflow jobs run
    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions!"
      - name: Show date
        run: date
      - name: Show operating system
        run: uname -a
